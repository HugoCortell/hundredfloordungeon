# How to compile (build) a CS file into a useful program.

1. CD your way to wherever your cs file is. Or where you want it to build.
2. Open new terminal (Terminal-> New Terminal) and type `dotnet new console`.
3. dotnet run
